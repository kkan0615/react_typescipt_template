import { createMuiTheme } from '@material-ui/core'

export const lightTheme = createMuiTheme({
  palette: {
    type: 'light',
    primary: {
      main: '#a7984f',
    },
    secondary: {
      main: '#dcc88c',
    },
    background: {
      default: '#db148c',
    },
  },
})
